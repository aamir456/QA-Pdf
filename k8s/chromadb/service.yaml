apiVersion: v1
kind: Service
metadata:
  name: chromadb
  namespace: pdf-qa
  labels:
    app.kubernetes.io/name: chromadb
    app.kubernetes.io/part-of: pdf-qa
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: chromadb
  ports:
    - name: http
      port: 8000
      targetPort: 8000
